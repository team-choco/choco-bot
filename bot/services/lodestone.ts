/**
 * Returns a character's url on lodestone.
 * @param id - the lodestone id
 */
export function character(id: number): string {
  return `https://na.finalfantasyxiv.com/lodestone/character/${id}`;
}
